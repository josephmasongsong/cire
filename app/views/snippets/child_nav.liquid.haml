%nav.nav.flex-column.side-nav
  {% for child in page.parent.children %}
  %a{class: "border-bottom text-primary p-3 {% if child.fullpath == page.fullpath %}active{% endif %}", href: "{% path_to child %}" } {{ child.title }}
  {% endfor %}